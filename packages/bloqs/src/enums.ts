export enum BloqCategory {
  Event = "event",
  Wait = "wait",
  Action = "action"
}

export enum BloqParameterType {
  Select = "select",
  SelectComponent = "selectComponent",
  Number = "number"
}

