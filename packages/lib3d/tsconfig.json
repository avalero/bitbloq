{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./build/dist",
    "rootDir": "src",
    "baseUrl": ".", // needed to use path aliases!
    "paths": {
      "three-full": ["node_modules/@types/three"]
    }
  },
  "include": ["./src"],
  "exclude": [
    "node_modules",
    "build",
    "scripts",
    "acceptance-tests",
    "webpack",
    "jest",
    "src/setupTests.ts"
  ]
}
